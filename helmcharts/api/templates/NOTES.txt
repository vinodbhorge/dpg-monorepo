{{ include "dpg-api.fullname" . }} deployed.

{{- if .Values.ingress.enabled }}
Reach the API via:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else if eq .Values.service.type "ClusterIP" }}
Port-forward to access locally:
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "dpg-api.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
{{- end }}

Required secret keys (rendered into Secret {{ include "dpg-api.secretName" . }}):
  AUTH_SECRET, POSTGRES_PASSWORD, REDIS_PASSWORD

Set them via --set secrets.data.AUTH_SECRET=... or use an existing secret:
  --set secrets.create=false --set secrets.existingSecret=my-secret
